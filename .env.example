# Docker Specific Env's Only - Can skip if needed

# Celery Config
REDIS_PORT=6379
FLOWER_PORT=5555

# Frontend Configuration
FRONTEND_PORT=3000
# URL of the FastAPI backend (e.g. http://localhost:8000)
NEXT_PUBLIC_FASTAPI_BACKEND_URL=http://localhost:8000
# Authentication mode: LOCAL or GOOGLE
NEXT_PUBLIC_FASTAPI_BACKEND_AUTH_TYPE=LOCAL
# ETL service: UNSTRUCTURED, LLAMACLOUD, or DOCLING
NEXT_PUBLIC_ETL_SERVICE=DOCLING

# Backend Configuration
BACKEND_PORT=8000

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=primus_idp
POSTGRES_PORT=5432

# pgAdmin Configuration
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@primus_idp.com
PGADMIN_DEFAULT_PASSWORD=primus_idp

